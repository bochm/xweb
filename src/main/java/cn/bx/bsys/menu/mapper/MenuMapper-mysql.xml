<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bx.bsys.menu.mapper.MenuMapper"> 
    <sql id="menuColumns">
    	a.id,
		a.parent_id "parent.id",
		a.parent_ids,
		a.name,
		a.target,
		a.icon,
		a.sort,
		a.type,
		a.permission,
		a.remarks,
		p.name AS "parent.name",
		a.status
    </sql>
    <sql id="menuJoins">
		LEFT JOIN sys_menu p ON p.id = a.parent_id
    </sql>
    <select id="listMenu" resultType="cn.bx.bsys.menu.mapper.Menu">
		SELECT
			<include refid="menuColumns"/>
		FROM sys_menu a
		<include refid="menuJoins"/>
		order by a.parent_ids
	</select>
	<select id="selectAllMenuTree" parameterType="map" resultType="map">
		select id,name,parent_id,icon icons from sys_menu
		<where>
			<if test="parentMenu != null and parentMenu != ''">
				parent_id = #{parentMenu}
			</if>
		</where>
	</select>
	<insert id="addMenu">
		insert into sys_menu(id,parent_id,parent_ids, name, target, icon, sort, type, permission, create_by, create_date, update_by, update_date, remarks, status)
		values(#{id},#{parent.id},#{parentIds},#{name},#{target},#{icon},#{sort},#{type},#{permission},#{createBy},#{createDate},#{updateBy},#{updateDate},
		#{remarks},#{status})
	</insert>
	
	
</mapper>